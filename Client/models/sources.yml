version: 2

sources:
  - name: fuente_bq
    description: Tablas en BigQuery adjuntadas como base bq en on-run-start
    database: bq
    schema: "{{ env_var('BQ_DATASET', 'your_bigquery_dataset') }}"
    tables:
      - name: customer
      - name: sale